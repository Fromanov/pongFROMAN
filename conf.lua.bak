-- Configuration

function love.conf(game)
  game.window.title = "Gay bars"
  game.window.width = 750
  game.window.height = 750      
  game.author = "Froman"       
  -- game.identity = nil             имя пути для сохранения (текст)
  --game.modules.audio = true     
  --game.modules.keyboard = true   -- включить модуль клавиатуры (логическое)
  --game.modules.event = true      -- включить модуль событий (логическое)
  --game.modules.image = true      -- включить модуль изображений (логическое)
  --game.modules.graphics = true   -- включить графический модуль (логическое)
  --game.modules.timer = true      -- включить модуль таймера (логическое)
  --game.modules.mouse = true      -- включить модуль мыши (логическое)
  --game.modules.sound = true      -- включить звуковой модуль (boolean)
  --game.modules.physics = true    -- включить модуль физики (boolean)
end